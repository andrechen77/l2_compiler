	(@main
		// comment here
		0
		// comment here
		rax <- 1
		rax <- mem %var 0
		mem %var 0 <- 0
		rcx += rdx
		%vasdfr <- stack-arg 8
		%var <<= rcx
		%vasdfr <<= 0
		mem %var 0 += 0
		rax += mem %var 0
		rax <- %vssdfar < rax
		cjump rax < 0 :label
		:label
		goto :label
		call @main 0
		call print 1
		%var++
		%vaagasfdr--
		%var @ %var %var 1
		// comment here
		return
		// comment here
	)